// SPDX-License-Identifier: ISC
pragma solidity ^0.5.0;

// The error shows in vscode is actually false alarm for the current version.
// The real reason for the error is that the compiler is not able to resolve

contract TodoList{
  uint public taskCount = 0;
}